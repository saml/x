Goal is to figure out Celery worker logging.

Currently, default logger is configured.
Task logger is not configured.
And nothing is logged when a task is requested.


```
pip install -r requirements.txt
python server.py
python worker.py
```

